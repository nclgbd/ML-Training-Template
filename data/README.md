## The data directory
This directory will host your data. Right now, it can be pulled from the data repo in the [link here]() directory.
You *can* change the format, however it's not recommended. But this is the folder where everything will be unzipped into.